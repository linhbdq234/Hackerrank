#include <bits/stdc++.h> //push_back: them 1 ki tu vao cuoi	vd push_back("*")
#include <iostream>		//pop_back: bot 1 ki tu cuoi
using namespace std;	// muon chen s.insert(chi so, " dau can chen ")
long long a[10000]; // muon xoa ki tu s,earse(chi so, so ki tu muon xoa) hoac substr
int main()			// muon tim kiem ki tu s.find("ki tu')
// string :: npos : khong tim thay
// reseve lat nguoc day so lai
{
	int a[]; //  cac ham co san isupper , islower , isalpha : chu cai , isdigit : chu so ,
	for(int i=0;i<n;i++)
	{
	cin >> a[i];
	}
	int cnt=0;
	for(int i=0;i<;i++)
	{
		if(abs((a[i]-a[i+1])!=1)
		{
			cnt=1;
		}
		else{
		cnt=0;
	}
	}
	if(cnt==1)	cout << "NO\n";
	else		cout << "YES\n";
	return 0;
	
}
