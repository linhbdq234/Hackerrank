#include <stdio.h>
#include <math.h>
int main()
{
int n;
cin >> n;
int cnt=0;
for(int i=1;i<=sqrt(n);i++)
{
  if(n%i==0)
{
int j=n/i;
if(i!=j)  cnt+=2;
else       cnt++; // truong hop i la can cua n thi chi liet ke 1 lan thoi k se bi trung
}
}
return 0;
}
